`sampling` <- function(N,
                       iter,
                       thinsteps,
                       response,  
                       sigma2,                   
                       K,
                       eta,
                       flin,
                       Ixx,
                       tildeX,
                       ttildeX,
                       hyperbsigma,
                       hyperaSigma2new,
                       S2,
                       drawTildeBeta,
                       drawTBML,
                       LL,
                       tbrq,
                       bm,
                       cL,
                       CE,
                       smstuff,
                       mustuff,
                       M,
                       rastuff,
                       R,
                       prob,
                       low,
                       high,
		       itcpt,
		       all)
	{
	.Call("sampling",
		as.integer(N),
		as.integer(iter),
		as.integer(thinsteps),
		as.numeric(response),
		as.numeric(sigma2),
		as.integer(K),
		eta,
		flin,
		Ixx,
		tildeX,
		ttildeX,
		hyperbsigma,
		hyperaSigma2new,
		S2,
		drawTildeBeta,
		drawTBML,
		as.integer(LL),
		tbrq,
		bm,
		cL,
		CE,
		smstuff,
		mustuff,
		as.integer(M),
		rastuff,
		as.integer(R),
		as.logical(prob),
		low,
		high,
		as.integer(itcpt),
		as.integer(all))
	}
